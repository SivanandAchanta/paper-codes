GWz = (Wz);
GWi = (Wi);
GWf = (Wf);
GWo = (Wo);

GRz = (Rz);
GRi = (Ri);
GRf = (Rf);
GRo = (Ro);

Gbz = (bz);
Gbi = (bi);
Gbf = (bf);
Gbo = (bo);

Gpi = (pi);
Gpf = (pf);
Gpo = (po);

GU  = (U);
Gbu = (bu);

GpdWz = (zeros(size(Wz)));
GpdWi = (zeros(size(Wi)));
GpdWf = (zeros(size(Wf)));
GpdWo = (zeros(size(Wo)));

GpdRz = (zeros(size(Rz)));
GpdRi = (zeros(size(Ri)));
GpdRf = (zeros(size(Rf)));
GpdRo = (zeros(size(Ro)));

Gpdbz = (zeros(size(bz)));
Gpdbi = (zeros(size(bi)));
Gpdbf = (zeros(size(bf)));
Gpdbo = (zeros(size(bo)));

Gpdpi = (zeros(size(pi)));
Gpdpf = (zeros(size(pf)));
Gpdpo = (zeros(size(po)));

GpdU  = (zeros(size(U)));
Gpdbu = (zeros(size(bu)));
