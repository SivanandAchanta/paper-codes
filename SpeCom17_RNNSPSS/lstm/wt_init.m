Wz = si*randn(nl(2),nl(3));
Wi = si*randn(nl(2),nl(3));
Wf = si*randn(nl(2),nl(3));
Wo = si*randn(nl(2),nl(3));

Rz = sr*randn(nl(2),nl(2));
Ri = sr*randn(nl(2),nl(2));
Rf = sr*randn(nl(2),nl(2));
Ro = sr*randn(nl(2),nl(2));

bz = zeros(nl(2),1);
bi = zeros(nl(2),1);
bf = zeros(nl(2),1);
bo = zeros(nl(2),1);

pi = zeros(nl(2),1);
pf = zeros(nl(2),1);
po = zeros(nl(2),1);


U  = su*randn(nl(3),nl(2));
bu = zeros(nl(3),1);
